<div class="container">
  <h4 class="m-2 my-4"><span class="bg-light">ログイン</span></h4>

  <h6 class="m-2 my-4"><span class="bg-light">会員の方はこちらからログイン</span></h6>

  <div class="container col-9">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="row mb-3">
        <%= f.label :"メールアドレス", class: "col-3 col-form-label"  %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control col-5", placeholder: "sample@example.com" %>
      </div>

      <div class="row mb-3">
        <%= f.label :"パスワード", class: "col-3 col-form-label" %>
        <%= f.password_field :password, autocomplete: "current-password", class: "form-control col-5"  %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="field">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
        </div>
      <% end %>

      <div class="row mt-3 mb-5">
        <div class="col-3"></div>
        <%= f.submit "ログイン", class: "col-2 btn btn-primary" %>
      </div>
    <% end %>
  </div>


  <h6 class="m-2 my-4">
    <span class="bg-light">会員登録がお済みでない方</span>
  </h6>
  <span class="mr-3">
    <u><%= link_to "こちら", new_customer_session_path %></u>
  </span>
  から新規登録を行ってください。
</div>
