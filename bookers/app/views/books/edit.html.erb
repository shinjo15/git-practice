
<h1 class="view-title">Editing Book</h1>

<% if @book.errors.any? %>
 <%= @book.errors.count %>error prohibited this book from being saved:
 <ul>
  <% @book.errors.full_messages.each do |message|%>
   <li><%= message %></li>
  <% end %>
 </ul>
<% end %>


<%= form_with model: @book, url: update_book_path(@book.id), mehod: :patch do |f|%>

<div class="field">
 <p>Title</p>
 <%= f.text_field :title %> 
</div>

<div class="field"> 
 <p>Body</p>
 <%= f.text_area :body %> 
</div>

<div class="actions">
 <%= f.submit 'Update Back'%>
</div>
 
 
 <%= link_to "show", book_path(@book.id)%>
 <%= link_to "Back", books_path %>
 
 <% end %>
